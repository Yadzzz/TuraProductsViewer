@page "/"

<PageTitle>Tura Sales Management</PageTitle>

<div class="page-header">
    <div class="page-title">
        <h3>Tura Sales Management</h3>
    </div>
</div>


<div class="row layout-top-spacing" id="cancel-row">
    <div class="col-xl-12 col-lg-12 col-sm-12  layout-spacing">
        <div class="widget-content widget-content-area br-6">

            <div class=""><p>Navigate to the right section through the topbar.</p></div>

        </div>
    </div>

</div>

@code
{
    protected override void OnInitialized()
    {
        
    }

    private void getprices()
    {
        //string[] ids = new string[] { "280006", "1030", "780032" };

        //Navision.PrepareRequest(ids, "2090", "NOK");

        //var dict = Navision.GetPrices();

        //foreach (var d in dict)
        //{
        //    Console.WriteLine(d.Key + " : " + d.Value);
        //}
    }
}